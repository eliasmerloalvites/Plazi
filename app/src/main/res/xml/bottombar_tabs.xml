<?xml version="1.0" encoding="utf-8"?>
<tabs>
    <tab
        id="@+id/search"
        title= "@string/tab_search"
        barColorWhemSelected="@color/colorPrimary"
        icon="@drawable/search"
        />
    <tab
        id="@+id/home"
        title= "@string/tab_home"
        barColorWhemSelected="@color/colorPrimary"
        icon="@drawable/home"
        />
    <tab
        id="@+id/profile"
        title= "@string/tab_profile"
        barColorWhemSelected="@color/colorPrimary"
        icon="@drawable/user"
        />
</tabs>